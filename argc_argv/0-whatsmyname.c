#include <stdio.h>

/**
 * main - Prints the name of the program
 * @argc: Arguments count
 * @argv: Array of args
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	argc;

	printf("%s\n", argv[0]);
	return (0);
}
